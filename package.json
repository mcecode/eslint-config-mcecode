{
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm test && npm run lint && node scripts/build.js",
    "format": "prettier . -w",
    "lint": "eslint . --cache --cache-location node_modules && prettier . -c",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^4.0.1",
    "eslint": "^8.2.0",
    "eslint-config-mcecode": "^2.0.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "2.5.1"
  }
}
