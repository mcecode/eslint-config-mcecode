{
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm test && npm run lint && node scripts/build.js",
    "format": "prettier . -w",
    "lint": "eslint . --cache --cache-location node_modules && prettier . -c",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^5.1.1",
    "eslint": "^8.32.0",
    "eslint-config-mcecode": "^3.0.0",
    "eslint-config-prettier": "^8.6.0",
    "prettier": "2.8.3"
  }
}
